package com.upc.backend.cargaSinEstres.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v1/") // esto viene en el enunciado
public class BookingHistoryController {

    //me falta completar esto - eric
    //para sus archivos guiense del ejemplo del lunes
    //el caso del modelo de BookingHistory fue especial ya que habian entidades anidadas
}
